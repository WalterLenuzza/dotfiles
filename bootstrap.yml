- name: Configure system
  hosts: workstation
  gather_facts: yes
  roles:
  - packages
  - system
  vars_prompt:
  - name: "ansible_sudo_pass"
    prompt: "[sudo] password"
    private: yes

- include: preferences.yml
